# Agents Workspace

This workspace contains business-lead agents under `agents/`, plus a configurable runner and console.

See [agents/README.md](agents/README.md) for the full documentation, setup flow, app URLs, and start commands.
